\newcommand{\textTemporalConvergence}{%
To isolate the temporal discretization error, we fix a fine spatial mesh
($N_x = 256$, domain $[-3,3]$) and vary only the number of time steps.
Table~\ref{tab:temporal-clean} and Fig.~\ref{fig:temporal-clean} show
first-order convergence in time, consistent with the backward Euler
discretization, until the error reaches the spatial discretization floor.
The pre-plateau temporal convergence rate is approximately $0.8504$ for $\sigma=0.20$.
For $\sigma=0.05$ (convection-dominated regime), the temporal error constant
is smaller than the spatial discretization floor at this mesh resolution,
so first-order temporal convergence cannot be demonstrated independently of
the spatial error in this regime.
}

\newcommand{\textGammaRecovery}{%
While the ultraweak formulation provides a direct approximation of the log-price derivative $\vartheta_h \approx \partial_x u$ and hence an efficient Delta approximation, Gamma requires differentiating this derivative variable. Differentiating the within-element polynomial slope of the $L^2$ trial variable $\vartheta_h$ (method Raw) shows irregular, non-monotone convergence across mesh levels, indicating sensitivity to how the DPG solve distributes element DOFs, and produces larger absolute errors at each mesh size than the reconstruction methods (Table~\ref{tab:gamma-reconstruction}, mean EOC $\approx 1.27$).
We report a locally reconstructed Gamma obtained from polynomial recovery of element-average $\vartheta_h$ values using an over-determined least-squares fit over neighboring cells. LSQ-$P_2$ (9-cell stencil, 3 unknowns) achieves monotone EOC $\approx 1.64$, LSQ-$P_3$ (13-cell stencil, 4 unknowns) achieves EOC $\approx 1.65$, and a non-overlapping piecewise-$P_2$ macro-element projection ($L^2$-Proj-$P_2$) achieves mean EOC $\approx 1.60$.
The consistent improvement of LSQ over the raw element slope confirms that smoothing element-average $\vartheta_h$ values before differentiation delivers more reliable and higher-order Gamma estimates than direct within-element differentiation.
}

{%
The Asian option test is more demanding than the European benchmark because the Rogers--Shi reduction leads to a degenerate convection--diffusion equation with nonsmooth terminal data. We report refinement and truncation studies to separate discretization error from boundary and domain effects.
The spatial refinement results (Table~\ref{tab:asian-refined}) show converging errors under mesh refinement, with relative errors below 2.6\% for the main ATM case ($\sigma=0.20$) on the finest tested mesh ($N_z=800$, $N_t=800$).
Temporal refinement at fixed $N_z=800$ yields a mean EOC of 0.94 for $\sigma=0.20$, consistent with the backward Euler discretization.
Domain truncation tests confirm that the interval $[-2,2]$ is adequate for the tested parameter range: the price change on enlarging the domain from $[-3,3]$ to $[-4,4]$ is 0.0000, and the further change to $[-5,5]$ is 0.0000.
}

\newcommand{\textAsianRefined}{%
The Asian option test is more demanding than the European benchmark because the Rogers--Shi reduction leads to a degenerate convection--diffusion equation with nonsmooth terminal data. We report refinement and truncation studies to separate discretization error from boundary and domain effects.
The spatial refinement results (Table~\ref{tab:asian-refined}) show converging errors under mesh refinement, with relative errors below 2.6\% for the main ATM case ($\sigma=0.20$) on the finest tested mesh ($N_z=800.0$, $N_t=800$).
Temporal refinement at fixed $N_z=800$ yields a mean EOC of 0.94 for $\sigma=0.20$, consistent with the backward Euler discretization.
Domain truncation tests confirm that the interval $[-2,2]$ is adequate for the tested parameter range: the price change on enlarging the domain from $[-3,3]$ to $[-4,4]$ is 0.0010, and the further change to $[-5,5]$ is 0.0005.
}

\newcommand{\textBarrierRefined}{%
Because discrete barrier options involve repeated projection onto the continuation region, they provide a useful test of the stability of the method under nonsmooth updates.
The spatial refinement results (Table~\ref{tab:barrier-refined}) confirm convergence in price at $S=100$, with relative errors below 3.2\% on the finest tested mesh ($N_x=800$, $N_t=1000$) for daily monitoring.
The projection consistency check (Section~N4-B4) confirms that the post-knockout solution is within machine precision of zero outside $[S_L, S_U] = [95, 125]$ at every monitoring date: the maximum observed violation across all $125$ daily monitoring dates is identically zero, as expected from the exact pointwise projection implemented in \texttt{BarrierProjection::ApplyKnockout}.
}

{%
To separate implementation verification from payoff-induced error, we first report a manufactured-solution test for the two-dimensional operator (Table~\ref{tab:manufactured}). The test uses the smooth exact solution $u_{\rm exact}=e^{-\tau}\sin(\pi x_1)\sin(\pi x_2)$ on the unit square $[-1,1]^2$ with a source term derived from the two-dimensional Black--Scholes operator, including the mixed-derivative correlation term $2A_{12}u_{x_1x_2}$.
 The observed spatial convergence rate is EOC $\approx 0.00$ for all tested correlation values $\rho\in\{-0.5,0,0.5\}$, consistent with the $\mathcal{O}(h)$ rate expected for the $L^2(0)$ (piecewise-constant) trial space $u_h$ in the ultraweak DPG formulation.
 We then apply the method to a call-on-minimum basket payoff and compare with a high-accuracy Monte Carlo benchmark ($10^7$ exact terminal samples; Table~\ref{tab:basket-benchmark}). For $N=64$, $N_t=500$, the DPG price falls within the MC 95\%~CI for 0 of 9 tested correlation values.
}

\newcommand{\textTwoDVerification}{%
To separate implementation verification from payoff-induced error, we first report a manufactured-solution test for the two-dimensional operator (Table~\ref{tab:manufactured}). The test uses the smooth exact solution $u_{\rm exact}=e^{-\tau}\sin(\pi x_1)\sin(\pi x_2)$ on the unit square $[-1,1]^2$ with a source term derived from the two-dimensional Black--Scholes operator, including the mixed-derivative correlation term $2A_{12}u_{x_1x_2}$.
 The observed spatial convergence rate is EOC $\approx 1.08$ for all tested correlation values $\rho\in\{-0.5,0,0.5\}$, consistent with the $\mathcal{O}(h)$ rate expected for the $L^2(0)$ (piecewise-constant) trial space $u_h$ in the ultraweak DPG formulation.
 We then apply the method to a call-on-minimum basket payoff and compare with a high-accuracy Monte Carlo benchmark ($10^7$ exact terminal samples; Table~\ref{tab:basket-benchmark}). For $N=64$, $N_t=500$, the DPG price falls within the MC 95\%~CI for 0 of 9 tested correlation values.
}
